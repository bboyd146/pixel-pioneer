services:
  - type: static
    name: pixel-pioneer
    buildCommand: npm install; npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html